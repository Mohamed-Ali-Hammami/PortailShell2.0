package com.tn.shell.gestat.serviceftp;

import java.text.ParseException;
import java.util.ArrayList;
import java.util.Date;
import java.util.List;

 

public class Test {
/*
 *     List<Achatcaisse> l3=new ArrayList<Achatcaisse>();
       l3=serviceAchatcaisse.getAll();
        List<Achatcaisse> l4=new ArrayList<Achatcaisse>();
        listAchatESP=new ArrayList<Achatcaisse>();
        try {
			 
    		 
     		for (Achatcaisse f : l3) {
     			if ( date1 != null){
     			Date d1 = dateFormat.parse(dateFormat.format(date1));
     			 
     			 if (d1.compareTo(dateFormat.parse(dateFormat.format(f.getDate())))<=0) 
     			{
     		      l4.add(f);
     			 }}
     			 
     			if(date1==null  ) l4.add(f);
     		}
     		for (Achatcaisse f : l4) {
     			if ( date2 != null){
     			Date d2 = dateFormat.parse(dateFormat.format(date2));
     				 
     			 if (  d2.compareTo(dateFormat.parse(dateFormat.format(f.getDate())))>=0) 
     				{
     				listAchatESP.add(f);
     				
     				}
     			}
     				if(  date2==null) listAchatESP.add(f);
     			}
     		
     			  
     		} catch (ParseException e1) {
     			// TODO Autof-generated catch block
     			e1.printStackTrace();
     		}
        
        listachat=new ArrayList<Achat>();
        List<Achat>l5=new ArrayList<Achat>();l5=serviceAchat.getAll();
        List<Achat>l6=new ArrayList<Achat>();
        
        try {
			 
   		 
     		for (Achat f : l5) {
     			if ( date1 != null){
     			Date d1 = dateFormat.parse(dateFormat.format(date1));
     			 
     			 if (d1.compareTo(dateFormat.parse(dateFormat.format(f.getDate())))<=0) 
     			{
     		      l6.add(f);
     			 }}
     			 
     			if(date1==null  ) l6.add(f);
     		}
     		for (Achat f : l6) {
     			if ( date2 != null){
     			Date d2 = dateFormat.parse(dateFormat.format(date2));
     				 
     			 if (  d2.compareTo(dateFormat.parse(dateFormat.format(f.getDate())))>=0) 
     				{
     				listachat.add(f);
     				
     				}
     			}
     				if(  date2==null) listachat.add(f);
     			}
     		
     			  
     		} catch (ParseException e1) {
     			// TODO Autof-generated catch block
     			e1.printStackTrace();
     		}
 * */
}
